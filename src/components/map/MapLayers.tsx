import { createPastureLayer } from './PastureLayer';
import { createHerdLayer } from './HerdLayer';
import { createWaterLayer } from './WaterLayer';
import { createDangerZoneLayer } from './DangerZoneLayer';
import { createNDVILayer } from './NDVILayer';

export const MapLayers = {
  createPastureLayer,
  createHerdLayer,
  createWaterLayer,
  createDangerZoneLayer,
  createNDVILayer,
};

export default MapLayers;
